#defmodule ExTwilio.ConfigTest do
  #use ExUnit.Case
  #alias ExTwilio.Config

  #test ".account_sid is present" do
    #assert Config.account_sid != nil
  #end

  #test ".auth_token is present" do
    #assert Config.auth_token != nil
  #end

  #test ".api_domain is present" do
    #assert Config.api_domain != nil
  #end

  #test ".api_version is present" do
    #assert Config.api_version != nil
  #end

  #test ".base_url is a combination of api_domain, api_version, and account_sid" do
    #assert Config.base_url == "https://#{Config.api_domain}/#{Config.api_version}/Accounts/#{Config.account_sid}/"
  #end
#end
